// Evaluation runs only in a local Node.js process. This no-op alias lets the
// harness reuse production server modules without weakening their Next.js
// `server-only` boundary in the application build.
export {};
