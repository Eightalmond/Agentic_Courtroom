# Trial by User does not require environment variables yet.
#
# Future server-side variables should be documented in this section. Never
# prefix secrets with NEXT_PUBLIC_, because that exposes them to the browser.
#
# Server-side services (future)
# VARIABLE_NAME=
