# Required only when running a synthetic customer simulation.
# Keep both values server-side. Never prefix them with NEXT_PUBLIC_.
OPENAI_API_KEY=
OPENAI_MODEL=

# No other environment variables are currently required.
# Add future server-side settings below without including real secrets.
